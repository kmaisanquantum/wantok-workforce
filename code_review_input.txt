File: backend/server.js
Added environment validation at the top:
if (!process.env.DATABASE_URL) {
  console.error("❌ CRITICAL CONFIG ERROR: DATABASE_URL is not defined in the environment.");
  console.error("💡 Recommendation: Check your Coolify environment variables or local .env file.");
  process.exit(1);
}

File: backend/db/db_init.js
1. Added robustness check for DATABASE_URL in initializeDatabase.
2. Modified the fallback loop to parse DATABASE_URL inside the loop and correctly handle the port:
   const config = parse(process.env.DATABASE_URL);
   if (host) { config.host = host; }
   const targetPort = config.port;
3. Updated logging to use `targetPort || "default"` instead of a hardcoded "5432".
