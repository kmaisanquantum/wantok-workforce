[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci", "cd backend && npm install"]

[phases.build]
cmds = ["NODE_OPTIONS=--max-old-space-size=4096 npm run build"]
dependsOn = ["install"]

[start]
cmd = "npm run start:prod"

[providers.node]
version = "22"
